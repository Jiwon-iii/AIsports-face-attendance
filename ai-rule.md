# ai-rule.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트를 위한 최소 기술 규칙이다.

이 문서는 실제 서비스에 바로 배포 가능한 코드를 생성하기 위한 최소 필수 기준이다.  
학습용, 예제용, 임시 코드는 허용하지 않는다.

이 규칙은 새 코드 작성 및 구조 설계 시 기본 기준으로 적용한다.  
기존 코드 유지, 분석, 부분 수정 시에는 과도한 리팩터링을 하지 않는다.

## 1. 고정 기술 스택

- 프레임워크: Next.js (App Router)
- 언어: TypeScript
- UI 스타일링: Tailwind CSS
- 데이터베이스: MongoDB + Mongoose
- 품질 게이트: ESLint (`npm run lint` 통과 필수)

사용자에게 명시적으로 요청받지 않는 한 프레임워크, 언어, 스타일링, 데이터베이스를 변경하지 않는다.

## 2. 코드 품질 규칙

- `any` 사용을 최소화하고, 명시적인 타입/인터페이스를 사용한다.
- 코드는 모듈화하고 단일 책임 원칙을 유지한다.
- 중복 로직을 피하고, 필요한 경우 공통 로직을 분리한다.
- 상태값, 임계값, 매직넘버는 상수 또는 설정값으로 관리한다.
- 주석은 의도가 불명확한 로직에만 짧게 작성한다.

## 3. 프로젝트 구조 규칙

- 공통 유틸리티: `src/lib`
- 데이터 모델/스키마: `src/models`
- API 라우트: `src/app/api`
- UI 컴포넌트와 DB/비즈니스 로직을 분리한다.

## 4. API 및 데이터 규칙

- 안정적인 식별 키로 `userId`를 사용한다.
- 핵심 컬렉션: `users`, `faceProfiles`, `attendanceRecords`, `consents`
- 출석 상태값은 반드시 `SUCCESS`, `FAILED`, `MANUAL`을 포함한다.
- 시간 데이터는 UTC ISO 형식으로 저장한다.
- DB 저장 전 요청 payload를 검증한다.

## 5. 보안 및 개인정보 규칙

- 민감정보(원본 얼굴 이미지, 토큰, 시크릿, 비밀번호)를 로그에 남기지 않는다.
- 원본 얼굴 이미지보다 얼굴 임베딩 저장을 우선한다.
- `.env*` 파일은 절대 커밋하지 않는다.
- 동의 기록 없이 얼굴 데이터를 처리하거나 저장하지 않는다.
- 시크릿 값은 환경변수로만 관리한다.

## 6. 안면인식 가드레일

- 자동 인식 실패 시 수동 출석 fallback을 반드시 제공한다.
- 매칭 임계값은 하드코딩하지 않고 설정값으로 관리한다.
- liveness 검증 없이 보안이 완성되었다고 주장하지 않는다.
- 측정 근거 없이 정확도나 성능을 단정하지 않는다.

## 7. 변경 관리 규칙

- 작고 검토 가능한 단위로 변경한다.
- 기존 동작 변경 시 영향 범위를 설명한다.
- API, 스키마, 라우트 변경 시 문서를 함께 갱신한다.

## 8. 금지

- 스키마 없는 컬렉션 접근
- 깊은 중첩 배열 구조
- 불필요한 `populate`

## 9. React / Component 규칙

- `'use client'`는 필요한 경우에만 사용
- 로딩, 에러, 빈 상태 UI 필수
- 비동기 로직은 handler 또는 hook으로 분리
- `useEffect` 남발 금지
- 파생 상태는 계산으로 처리 (`state` 금지)

## 10. Custom Hook 규칙

- 재사용 가능하게 설계한다.
- 상태는 `data`, `isLoading`, `error`로 명확히 분리한다.
- fetch 로직은 하나의 함수로 캡슐화한다.
- `useCallback`으로 참조 안정성을 확보한다.

```ts
return { data, isLoading, error, refetch };
```

## 11. AI 출력 기준 (중요)

AI는 항상 아래 기준으로 출력한다:

- "왜 이런 구조인지"에 대한 설명은 최소화
- 코드가 스스로 설명되도록 작성
- 실무 코드 리뷰에서 지적당할 요소 제거
- 바로 커밋 가능한 상태로 출력

## 12. 에러 처리 원칙

- 모든 실패 케이스는 사용자 메시지와 개발자 메시지를 구분한다.
- 사용자 메시지는 명확하게 전달하되, 내부 정보는 노출하지 않는다.
- 서버 로그에는 context를 포함한다.

```ts
console.error("[POST /api/teams]", error);
```
